https://leetcode.com/problems/binary-tree-inorder-traversal/description/

recursion - time O(n), space height

morris - iteration, does not take space
time O(n), space O(1)


class Solution {
  public List<Integer> inorderTraversal(TreeNode root) {
    List<Integer> retVal = new ArrayList<>();

    TreeNode curr = root;

    while (curr != null) {
      if (curr.left == null) {
        retVal.add(curr.val);
        curr = curr.right;
      } else {
        TreeNode iop = curr.left;
        while (iop.right != null && iop.right != curr) {
          iop = iop.right;
        }

        if (iop.right == null) {
          iop.right = curr; // making the link
          curr = curr.left;
        } else {
          iop.right = null;
          retVal.add(curr.val);
          curr = curr.right;
        }
      }
    }

    return retVal;
  }
}


Morris Traversal (Inorder) - Node

Case 1: node.left == null
print node
go to right

Case 2: node.left != null
left not processed (left ka rightmost)
iop.right == null
iop.right = node (return path)
go to left

Case 3: node.left != null
left is processed (left ka rightmost)
iop.right == node
iop.right = null
print node
go to right
