select last_name
from actor;

select last_name as LastName
from actor;

select * 
from film;

select rental_duration
from film;

select rental_duration,
rental_duration * 24 as rent_dur_hours
from film;

select
3*3 as multiplication,
6+6 as addition,
1-1 as subtraction,
10/2 as division;
  
select first_name, last_name,
first_name || ' ' || last_name as full_name
from customer;

select first_name, last_name, (left(first_name || ' ' || last_name, 2))
from customer;

select first_name, last_name, initcap(left(first_name || ' ' || last_name, 2))
from customer;

select first_name, last_name, (left(first_name || ' ' || last_name, 2))
from customer;

select first_name, last_name, lower(left(first_name || ' ' || last_name, 2))
from customer;

select first_name, last_name, lower(right(first_name || ' ' || last_name, 2))
from customer;

select title, rental_duration
from film
where rental_duration < 5;

select title, rental_duration
from film
where rental_duration = 5;

select title, rental_duration
from film
where rental_duration > 5;

select title, rental_duration
from film
where rental_duration <= 5;

select title, rental_duration
from film
where rental_duration >= 5;

select title, rental_duration
from film
where rental_duration != 5;

select title, rental_duration
from film
where rental_duration <> 5;

-- hi bro (-- is comment in sql)

select *
from customer c
where first_name = 'MARY';

select *
from rental r
where rental_date >= '2006-01-01';

select title, rental_duration, length
from film
where length > 100 and rental_duration = 5;

select title, rental_duration, length
from film
where length > 100 or rental_duration = 5;

select title, rental_duration, length
from film
where length > 100 and rental_duration = 5
and left(title, 1) = 'A';

select title, rental_duration
from film
where not rental_duration = 5;

-- both are same

select title, rental_duration
from film
where rental_duration != 5;

select title, rating
from film
where rating <> 'PG';

-- rating <> 'PG' means every rating except PG

select title, rating
from film
where rating is null;

-- can't use => where rating = 'NULL';

select title, rating
from film
where rating is not null;

select first_name, customer_id
from customer
where customer_id in (1, 8, 14, 44, 50);

select first_name, customer_id
from customer
where customer_id not in (1, 2, 3, 4, 5);

select first_name, customer_id
from customer
where customer_id between 1 and 8;

select first_name, customer_id
from customer
where customer_id not between 2 and 590;

select first_name
from customer
where left(first_name, 1) = 'A'
or left(first_name, 1) = 'F'
or left(first_name, 1) = 'J';

select first_name
from customer
where left(first_name, 1) in ('A', 'F', 'J');

select first_name
from customer
where first_name like 'M%';

-- starts with M, followed by anything (or nothing)

select first_name
from customer
where first_name like '_M';

-- exactly 2 characters, with M as the second character

select first_name
from customer
where first_name like '__M';

-- exactly 3 characters, with M as the third character

select first_name
from customer
where first_name like '_M_';

-- exactly 3 characters, with M as the second character

select first_name
from customer
where first_name like '__M_%';

select first_name
from customer
where first_name similar to 'M%L{2}%';

-- starts with M, followed by anything, then exactly 2 L's, followed by anything

select title, length
from film
order by length;

-- sorts the results by length in ascending order

select title, length
from film
order by length asc;

select title, length
from film
order by length desc;

select title, length
from film
where length > 184
order by length desc;

select title, length
from film
where length > 184 or length is null
order by length desc;

select title, length
from film
order by length
limit 5;

-- returns only the first 5 rows

select title, length
from film
order by length
offset 980;

-- skips the first 980 rows and returns the next rows to end

select distinct rating
from film;

-- removes duplicate ratings and returns each unique rating only

select distinct rating, length
from film;

-- removes duplicate ROW combinations of rating + length

select title, length,
case
	when length <= 60 then 'short'
	when length > 60 and length <= 120 then 'long'
	when length > 120 then 'very long'
	end as length_description
from film;

select title, length,
case
	when length <= 60 then 'short'
	when length > 60 and length <= 120 then 'long'
	when length > 120 then 'very long'
	else 'unknown'
	end as length_description
from film;

select count(*)
from film;

select count(length)
from film;

select count(distinct rating)
from film;

-- count does not count null

select distinct rating
from film;

select sum(length)
from film;

-- aggregate functions - count, sum, min, max, avg

select
min(amount),
max(amount),
avg(amount)
from payment;

select rating, count(*)
from film
group by rating;

-- counts how many films belong to each rating

select rating, title
from film
order by rating;

-- sorts films by rating in ascending order

select customer_id, staff_id, count(*)
from payment
group by customer_id, staff_id
order by customer_id, staff_id;

-- groups payments by customer and staff, then sorts by customer and staff

select customer_id, staff_id, count(*)
from payment
group by customer_id, staff_id
having count(*) > 20
order by customer_id, staff_id;

/*
from: pick the tables to be queried
where: filter the rows
group by: aggregate rows
having: filter the aggregates
select: select the columns that appear in the output
order by: sort the rows
limit: limit the number of returned rows
*/

select rating, count(*)
from film
where length > 60
group by rating
having count(*) > 10
order by rating
limit 5;

select *
from film
cross join store;

select film_id, store_id
from film
cross join store
order by film_id, store_id;

-- 1000 * 2 = 2000

select store_id
from store;

-- 2

select film_id
from film;

-- 1000

select rental_date, first_name, last_name
from rental
inner join customer
on rental.customer_id = customer.customer_id;

select film_id, store_id
from film
inner join store
on film.film_id = store.store_id;

select film_id, store_id
from film
left join store
on film.film_id = store.store_id;

select film_id, store_id
from film
right join store
on film.film_id = store.store_id;

select film_id, store_id
from film
full outer join store
on film.film_id = store.store_id;

/*
cross join: Cartesian product
inner join: cross join + filtering
outer join: cross join + filtering + add missing rows

my understanding:
cross join: every possible combination of rows
inner join: only matching rows
outer join: matching rows + unmatched/missing rows
*/

select title, length
from film
where length > (select avg(length) from film)
order by length;

select customer_id,
       sum(amount) as cust_amount,
       100.0 * sum(amount) / (select sum(amount) from payment) as pct
from payment
group by customer_id
order by pct desc;

select title, rating
from film
where rating in
(select distinct rating
    from film
    where left(cast(rating as text), 2) = 'PG');

