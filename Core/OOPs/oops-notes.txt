OOPS - Encapsulation, Inheritance, Polymorphism
Encapsulation - Classes, Objects

Encapsulation-
The process of creating classes And putting data members and functions together in classes is called encapsulation.

Classes-

Objects-

new Person krne se heap mai ek object(instance) bn gya (jiske andar uss object ki properties padi hai lekin sb 0 ya null hai) jiska address(reference) stack mai stored hai

jis object pr aapne function call kia hai toh this apne aap pass ho jata hai aur this ke andar uss object ka address pass ho jata hai
this reference hota hai (address)

Instance Reference-
heap ke andar object bnta hai - Instance/Objects
uska address stack mai store hota hai - Reference

Static data members - class ko belong krte hai
jb hum class bnate hai
static class BankAccount{
  int anum;
  int money;
  static int roi;
}

static int roi; // jis cheez ko static bna dete hai wo class se kbhi object mai aata hi nhi hai wo class mai hi rehta hai

BankAccount a1 = new BankAccount(); // ye krne se har baar bank account ka object bnega aur usme anum, money hoga but (static int roi) nhi hoga wo uss class se har baar object bnate hue nhi bnega wo bs ek hi hai jo class ko belong krta hai aur uski value change krni h toh 
BankAccount.roi krke assign hoga

Static- used on three places
Static data members, Static functions, Static class*

wo function jisko call krne ke liye object bnana na pde
Static functions- Such functions which do not require object for their invocation (are invoked directly via class names)*
BankAccount.roi isko directly nhi ched skte, function bna kar ched skte

refer f1 file
Static functions implications - 
Q1. this keyword cannot be used in static functions. Why?
Ans - Because static functions can be called by class name in that case this won't know which instance to point to, it knows only when called using object

Q2. non-static data members can't be used in static functions. Why?
Ans - Because static functions can be called by class name and in that case we don't know if any object exists and if they do we wont know which objects non-static data members to use.

Q3. non-static functions can't be used in static functions. Why?
Ans - non-static functions can be used by making an object inside the static function and calling the nonstatic function on it. Can't be called directly.

4. can non-static function use static data member? Why?
Ans - Yes because we have extra information available not less

5. can non-static function use static function? Why?
Ans - Yes because we have extra information available not less

Inheritance & Polymorphism - 

--------------------------------------------------------------------------

agar kisi class mai koi constructor provided nhi hai toh java apni default constructor add krti hai

Object creation
1. Space allocation & this assignment
    Space is allocated for all non-static data members. this is set equal to the new object's address
    Data members are assigned default values (JAVA)
    0 = byte, short, int, long
    0.0 = float, double
    false = boolean
    '\0' = char
    null = everything else
2. Parsing
    Class creator's default values are assigned to data members (but only such values which are non-dynamic)
    e.g. growth multiple is set to 1.5 in ArrayList
    e.g. rehashing threshold is set in HashMap to 0.5
3. Constructor
    3.1 Default = Class creator's default values are assigned to data members (but only such values which are dynamic such as student folder or dbconnection)
    3.2 Parametrized = For class user to set values which he requires
    3.3 If no constructor is provided, Java adds the default ctor by itself
    3.4 If class creator, provides a Parametrized ctor (then Java won't add default ctor)

Inheritance & Polymorphism - 
  P and C - all cases Rules
    1. Compiler (LHS, Reference) compiles
    2. JRE (RHS, instance) and runs functions
    3. Conflict resolved by Reference
  1. P ref, P inst
  2. C ref, C inst
  3. P ref, C inst  (1 and 3 are polymorphic examples, function overriding)
  4. C ref, P inst => not allowed
  5. Run-time polymorphism -> calling fun in case 1 and 3 (fn overriding)
  6. Compile-time polymorphism -> Arrays.sort multiple (fn overloading)

--------------------------------------------------------------------------

1. Final 
  variable = values can't be reassigned
  data member = values can't be reassigned (either assign in parsing or constructor / static block)
  fn = Can't override the fn in derived class (overloading is allowed in same class and derived class)











10 july 2022------------------------------------------------------------------

Variable vs Data Member

Data Member:
A variable declared inside a class but outside any method/function is called a data member.
It belongs to the object/class and represents its properties or state.

Variable:
A variable declared inside a method/function/block is called a local variable.
It is used only within that method/function/block.

Example:

class Person {

    int age;          // data member
    String name;      // data member

    void display() {
        int salary;   // local variable
        String city;  // local variable
    }
}

final keyword - 

The `final` keyword is used to restrict changes.

1. final variable (local variable)

A final local variable can be assigned a value only once.
Once a value is assigned, it cannot be changed.

Example:

void fun() {
    final int x = 10;

    x = 20;   // ERROR
}


2. final data member

A final data member can be assigned a value only once.
Once initialized, its value cannot be changed.

Example:

class Person {

    final int age = 25;   // final data member

    void fun() {
        age = 30;         // ERROR
    }
}


3. final function (method)

A final method cannot be overridden by a child class.

Example:

class Parent {

    final void fun() {
        // code
    }
}

class Child extends Parent {

    void fun() {
        // ERROR: cannot override final method
    }
}

Easy way to remember:

final variable  → value cannot be changed
final data member → value cannot be changed
final method → cannot be overridden

data member = values can't be reassigned (either assign in parsing or constructor / static block)
functions = can't override the fn in derived class (overloading is allowed in same class and derived class)
class = can't derive the final class











16 july 2022------------------------------------------------------------------



Abstract
    class = can't make an object of abstract class
    (for instance Employee class derived into HREmployee or Software Employee. Employee class should be abstracrt, because not a type in itself, just created to absorb common functions of HREmployee and SoftwareEmployee)
    fn = fn which doesn't have code (only API)
    implications
        Why required? -> To absorb common code of deriving Classes
        If fn is abstract, will the containing class be necessarily abstract? Yes
        If class is abstract, will it necessarily have abstract functions? No


interface
    -> When all fns are abstract and there are no common instance data members to share
    -> Similar to abstract classes
    -> An extreme version of abstract classes
        -> when all fns are abstract
        -> no common instance data members
    -> If a class implements an interface but doesn't provide body for all functions, then it will become abstract
    -> Can we make an object of interface? No
    -> Can an interface provide body for any function? Yes, since Java8, using the default keyword it is possible (although advised to not use). Java provided for this backword compatibility. Not to be used often.
    -> Can an interface have data members? Yes, but not as instance data members, can be used as static data members


abstract class vs interface (4:32 to 4:42 for notes)
    -> Abstract classes can have implementations for some functions, interfaces are supposed to have all functions with no implementations
    -> Abstract classes can have instance data members, interfaces can only have static data members
    -> A class can't derive two abstract classes, but can implement any number of interfaces
    -> Interface data members are final and static

Questions
    Q1. What is final variable?
    Q2. What is final data member?
    Q3. When are they initialized?
    Q4. What is final fn? -> Can't override them in derived class
    Q5. What is a final class? -> Can't derive the final class
    Q6. What is abstract class? -> Can't make object
    Q7. What is abstract fn? -> A fn with no body.
    Q8. Can an abstract class have 0 abstract fns? -> Yes
    Q9. Can a class having abstract fns be market non-abstract? -> False. Have to mark the class abstract or provide implementations for all abstract fns
    Q10. Ctor in abstract class? -> allowed
    Q11. Why abstract fn? -> TO enforce the deriving classes to provide implementations
    Q12. Why abstract class? -> To represent the common concept and absorb common functionality or data memebrs?
